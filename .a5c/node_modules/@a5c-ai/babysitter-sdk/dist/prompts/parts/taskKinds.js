"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.renderTaskKinds = renderTaskKinds;
const templateRenderer_1 = require("../templateRenderer");
/**
 * Renders the Task Kinds table and Effect Execution Hints section.
 */
function renderTaskKinds(ctx) {
    const skillSystemLabel = ctx.harness === 'claude-code'
        ? 'Claude Code skill'
        : 'Installed skill';
    const augmentedCtx = {
        ...ctx,
        skillSystemLabel,
    };
    return (0, templateRenderer_1.renderTemplate)((0, templateRenderer_1.resolveTemplatePath)('task-kinds.md'), augmentedCtx);
}
