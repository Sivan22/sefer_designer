"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.renderQuickReference = renderQuickReference;
const templateRenderer_1 = require("../templateRenderer");
/**
 * Renders the Quick Commands Reference section with harness-specific flags.
 */
function renderQuickReference(ctx) {
    const bindingFlags = ctx.sessionBindingFlags
        ? ` \\\n  ${ctx.sessionBindingFlags}`
        : '';
    const augmentedCtx = {
        ...ctx,
        bindingFlags,
    };
    return (0, templateRenderer_1.renderTemplate)((0, templateRenderer_1.resolveTemplatePath)('quick-reference.md'), augmentedCtx);
}
