"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.renderInterview = renderInterview;
const templateRenderer_1 = require("../templateRenderer");
/**
 * Renders the Interview phase section for both interactive and non-interactive modes.
 */
function renderInterview(ctx) {
    // Pre-compute the tool reference for the non-interactive header
    const toolRef = ctx.interactiveToolName
        ? ` or no ${ctx.interactiveToolName}`
        : '';
    const augmentedCtx = {
        ...ctx,
        interactiveToolName: toolRef,
    };
    return (0, templateRenderer_1.renderTemplate)((0, templateRenderer_1.resolveTemplatePath)('interview.md'), augmentedCtx);
}
