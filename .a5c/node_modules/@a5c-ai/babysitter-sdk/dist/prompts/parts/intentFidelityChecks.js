"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.renderIntentFidelityChecks = renderIntentFidelityChecks;
const templateRenderer_1 = require("../templateRenderer");
/**
 * Renders the Intent Fidelity Checks section.
 * Only returns content if ctx.hasIntentFidelityChecks is true (currently Codex only).
 */
function renderIntentFidelityChecks(ctx) {
    if (!ctx.hasIntentFidelityChecks) {
        return '';
    }
    return (0, templateRenderer_1.renderTemplate)((0, templateRenderer_1.resolveTemplatePath)('intent-fidelity-checks.md'), ctx);
}
