"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.renderIteration = renderIteration;
const templateRenderer_1 = require("../templateRenderer");
/**
 * Renders the Run Iteration section.
 */
function renderIteration(ctx) {
    const iterateFlagsSuffix = ctx.iterateFlags
        ? ` ${ctx.iterateFlags}`
        : '';
    return (0, templateRenderer_1.renderTemplate)((0, templateRenderer_1.resolveTemplatePath)('iteration.md'), ctx, {
        iterateFlagsSuffix,
    });
}
